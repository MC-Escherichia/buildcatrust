from setuptools import setup

setup(
    name="buildcatrust",
    version="0.1.0",
    description="Parse Certificate bundles to assemble trust stores",
    author="Matthew Conway",
    author_email="matthew.conway@regeneron.com",
    packages=["buildcatrust"],
)
